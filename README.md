# moodle-assignsubmission_bloboffload
Offload student submissions to an Azure Blob Storage Container to properly handle data lifecycles and reduce costs.

## Requirements
Moodle v5.2 (It uses the new React Subsystem so it is not backwards compatible with older Moodle versions)
